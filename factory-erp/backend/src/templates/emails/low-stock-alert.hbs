<!DOCTYPE html>
<html>

<head>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            color: #333;
        }

        .header {
            background: #ef4444;
            color: white;
            padding: 20px;
            text-align: center;
        }

        .item-list {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }

        .item-list th,
        .item-list td {
            padding: 10px;
            border: 1px solid #e5e7eb;
            text-align: left;
        }

        .warning {
            color: #ef4444;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h2>⚠️ Critical Low Stock Alert</h2>
        </div>
        <div class="content">
            <p>The following {{totalItems}} items have fallen below their minimum stock levels:</p>

            <table class="item-list">
                <thead>
                    <tr style="background: #f3f4f6;">
                        <th>Item Code</th>
                        <th>Name</th>
                        <th>Current Stock</th>
                        <th>Min Level</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each items}}
                    <tr>
                        <td><strong>{{this.code}}</strong></td>
                        <td>{{this.name}}</td>
                        <td class="warning">{{this.currentStock}} {{this.unit}}</td>
                        <td>{{this.minStock}} {{this.unit}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>

            <p>Please take immediate action to replenish inventory.</p>
            <a href="{{dashboardUrl}}"
                style="background: #ef4444; color: white; padding: 10px 20px; text-decoration: none; display: inline-block;">View
                Inventory</a>
        </div>
        <div class="footer">
            Generated by Inventory Management System - {{year}}
        </div>
    </div>
</body>

</html>