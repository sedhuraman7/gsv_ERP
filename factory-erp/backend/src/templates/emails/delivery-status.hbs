<!DOCTYPE html>
<html>

<head>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            color: #333;
        }

        .header {
            background: #2563eb;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 8px 8px 0 0;
        }

        .content {
            padding: 20px;
            border: 1px solid #e5e7eb;
            border-top: none;
            border-radius: 0 0 8px 8px;
        }

        .status-badge {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 20px;
            color: white;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 14px;
        }

        .status-delivered {
            background-color: #16a34a;
        }

        .status-failed {
            background-color: #dc2626;
        }

        .status-shipped {
            background-color: #ca8a04;
        }

        .details {
            margin-top: 20px;
            background: #f8fafc;
            padding: 15px;
            border-radius: 8px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h2>Delivery Update: Invoice #{{invoiceNo}}</h2>
        </div>
        <div class="content">
            <div style="text-align: center; margin: 20px 0;">
                <span class="status-badge status-{{cssClass}}">{{status}}</span>
            </div>

            <p>Hello,</p>
            <p>The delivery status for your order <strong>#{{invoiceNo}}</strong> has been updated.</p>

            <div class="details">
                <p><strong>Customer:</strong> {{customerName}}</p>
                <p><strong>Updated By:</strong> {{updatedBy}}</p>
                <p><strong>Time:</strong> {{timestamp}}</p>

                {{#if reason}}
                <p style="color: #dc2626;"><strong>Reason:</strong> {{reason}}</p>
                {{/if}}
            </div>

            {{#if showAction}}
            <div style="text-align: center; margin-top: 30px;">
                <a href="{{actionUrl}}"
                    style="background: #2563eb; color: white; padding: 10px 20px; text-decoration: none; border-radius: 6px;">View
                    Details</a>
            </div>
            {{/if}}
        </div>
        <div style="text-align: center; margin-top: 20px; color: #6b7280; font-size: 12px;">
            Generated by GSV Factory ERP
        </div>
    </div>
</body>

</html>